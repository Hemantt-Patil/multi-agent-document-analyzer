#  Multi-Agent Document Analyzer
A FastAPI + Next.js project that analyzes and summarizes uploaded documents using multi-agent architecture.

##  Run Backend
```bash```
cd backend
pip install -r requirements.txt
uvicorn main:app --reload

## Run Frontend
cd frontend
npm install
npm run dev

